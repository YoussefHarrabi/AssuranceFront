// role.enum.ts
export enum Role {
  CLIENT = 'CLIENT',
    ADMIN = 'ADMIN',
    RH = 'RH'
  }
  
  // identity-type.enum.ts
  export enum IdentityType {
    CIN = 'CIN',
    PASSPORT = 'PASSPORT',
  
  }